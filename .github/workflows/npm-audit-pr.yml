on: push

jobs:
  npm-audit-pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.0.0
      - uses: miyajan/npm-audit-pr@6622cc73df26bd2348a182316e9f8ff664e3dbc8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
