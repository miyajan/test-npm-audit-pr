on: push

jobs:
  npm-audit-pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.0.0
      - uses: miyajan/npm-audit-pr@c923ed7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
